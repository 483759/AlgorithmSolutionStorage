#include <cstdio>
int n, order[10], score[51][10], a[10];
int func() {
	for (int i = 0; i < n; i++) {
		int out = 0;
		if (out == 3)continue;
	}
}
void dfs(int dep) {
	for(int i=dep;i<=9;i++)
		order
}
int main() {
	scanf("%d", &n);
	order[4] = 1;
	for (int i = 1; i <= n; i++)
		for (int j = 1; j <= 9; j++)scanf("%d", &score[i][j]);

	return 0;
}